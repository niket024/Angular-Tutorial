import { DefaultInputDirective } from './default-input.directive';

describe('DefaultInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultInputDirective();
    expect(directive).toBeTruthy();
  });
});
