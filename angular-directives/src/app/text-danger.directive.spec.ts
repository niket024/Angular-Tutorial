import { TextDangerDirective } from './text-danger.directive';

describe('TextDangerDirective', () => {
  it('should create an instance', () => {
    const directive = new TextDangerDirective();
    expect(directive).toBeTruthy();
  });
});
