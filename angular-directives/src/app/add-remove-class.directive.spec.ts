import { AddRemoveClassDirective } from './add-remove-class.directive';

describe('AddRemoveClassDirective', () => {
  it('should create an instance', () => {
    const directive = new AddRemoveClassDirective();
    expect(directive).toBeTruthy();
  });
});
